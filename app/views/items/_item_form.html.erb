

<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this fashion item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
   <%= f.label :name %><br>
   <%= f.text_field :name %>
</div>
<div class="field">
  <%= f.label :classification %><br>
  <%= f.text_field :classification %>
</div>
<div class="field">
  <%= f.label :price_web %><br>
  <%= f.text_field :price_web %>
</div>
<div class="field">
  <%= f.label :currently_active %><br>
  <%= f.check_box :currently_active %>
</div>
 <div class="actions">
    <%= f.submit %>
 </div>
<% end %>


<%= link_to 'Back', items_path %>
